export const environment = {
  production: true,
  endpoints: {
    CATEGORIES_URL: '/api/categories', // todo: replace it with real api url fg: /api/categories
    PRODUCTS_URL: '/products' // todo: replace it with real api url fg: /products
  }
};
